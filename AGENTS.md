# 部署流程（固化）

每次用户要求上线、发布或部署时，按以下流程执行：

1. 保留用户当前 Chrome、Edge 和 Render 页面，不关闭或重开已有业务页面。
2. 接管用户已经登录的 Chrome，进入仓库的 GitHub 上传页面。
3. 使用 Finder 将本次需要发布的文件直接拖入 GitHub 上传区域。禁止使用 `Choose your files` 文件选择器。
4. 提交到 `main` 分支，提交后核对 GitHub 的最新提交和文件内容。
5. 等待 Render 自动部署；核对部署状态、健康接口和线上页面版本。
6. 若 GitHub 或 Render 网络失败，保留当前页面和本地改动，说明具体失败点，不重复尝试其他上传方式，也不关闭网页。

部署前先确认本次应发布的文件范围；用户说“全部上线”时发布完整项目，用户指定部分时只发布指定改动及其必要依赖。
